{
  "name": "debisa-daricha-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "test": "echo \"No test specified\" && exit 0"
  }
}

